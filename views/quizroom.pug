extends layout

block content
  h1 クイズルーム🚪
  a(href="/dashboard").btn.btn-outline-danger 退室
  div.quiz-div
    h2#alert-message 接続中です。少しお待ちください。リロードは不要です。
    h3#timer
    h2#quiz-name
    h4#quiz-by-username
    hr
    h1#question
    h1#answer
  h2 チャット
  hr
  div#message
  input(type="checkbox").answer-hidden
  label(for="answer-hidden") 　答えの投稿を表示する
  h3#user-count
  form(action="").chat
    input(type="checkbox")#is-answer
    label(for="is-answer") 　答えとして投稿する
    div.form
      input(autocomplete="off" placeHolder="チャットを入力...")#input.form-control
      button#form.btn.btn-outline-info 送信
      input(type="hidden" name=`${user.username}`)#username
  script(defer, src="/socket.io/socket.io.js")
  script(defer, src="/javascripts/quiz-chat.js")