extends layout

block content
  h1 #{user.username}
  p #{user.username} さんのユーザーページです。

  div.quiz-list
    h2.my-3 #{user.username} さんのクイズ一覧💬
    - var hasQuizzes = quizzes.length > 0
    if(hasQuizzes)
      div.table-warp
        table.table
          tr
            th クイズ名
            th 更新日時  
            th ⭐️
          each quiz in quizzes
            tr
              td
                a(href=`/quiz/${quiz.quizId}`) #{quiz.quizName}
              td #{quiz.updatedAt}
              td #{quiz.star}
    else 
      h4 クイズは見つかりませんでした😢