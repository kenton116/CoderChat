extends layout

block content
  h1 会話して、学ぼう。💬
  p リアルタイムチャットサービス CoderChat
  if user
    a(href="/dashboard") ダッシュボード
    br
    a(href="/logout") #{user.username} をログアウト
  else
    a(href="/login") ログイン